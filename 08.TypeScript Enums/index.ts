enum direction {
    North,
    East,
    West,
    south
}

let currentDirection = direction.North

console.log(currentDirection)

enum StatusCodes {
    NotFound = 404,
    Success = 200,
    Accepted = 202,
    BadRequest = 400
  }

console.log(StatusCodes.NotFound)